### This config file contains required core defaults that must be set, along with a handful of common optional settings.
### For a full list of available settings, see https://microsoft.github.io/graphrag/config/yaml/

### LLM settings ###
## There are a number of settings to tune the threading and token limits for LLM calls - check the docs.

models:
  default_chat_model:
    type: dashscope # 使用新创建的 dashscope 类型
    api_key: ${GRAPHRAG_API_KEY} # 使用阿里云百炼API Key
    model: qwen-plus # 使用通义千问模型
    # max_tokens: 800  # 减小最大token数，适应小块文本
    model_supports_json: true
    concurrent_requests: 25
    async_mode: threaded
    retry_strategy: native
    encoding_model: cl100k_base
    tokens_per_minute: auto
    requests_per_minute: auto
  default_embedding_model:
    type: openai_embedding # 使用openai_embedding类型，但配置为阿里云百炼
    api_base: https://dashscope.aliyuncs.com/compatible-mode/v1
    # api_version: 2024-01-01
    auth_type: api_key
    api_key: ${GRAPHRAG_API_KEY}
    model: text-embedding-v4 # 使用阿里云百炼的embedding模型
    model_supports_json: true
    concurrent_requests: 25
    async_mode: threaded
    retry_strategy: native
    max_retries: 10
    max_batch_size: 10
    batch_max_tokens: 2048
    encoding_model: cl100k_base
    tokens_per_minute: null
    requests_per_minute: null

### Input settings ###

input:
  storage:
    type: file # or blob
    base_dir: "input"
  file_type: text # [csv, text, json]
  

chunks:
  size: 1200
  overlap: 100
  group_by_columns: [id]  # 利用id进行分组，实现一对多，而不是多对多关系

### Output/storage settings ###
## If blob storage is specified in the following four sections,
## connection_string and container_name must be provided

output:
  type: file # [file, blob, cosmosdb]
  base_dir: "output"
    
cache:
  type: file # [file, blob, cosmosdb]
  base_dir: "cache"

reporting:
  type: file # [file, blob, cosmosdb]
  base_dir: "logs"

vector_store:
  default_vector_store:
    type: lancedb
    db_uri: output\lancedb
    container_name: default
    overwrite: True

### Workflow settings ###

embed_text:
  model_id: default_embedding_model
  vector_store_id: default_vector_store
  batch_size: 10
  batch_max_tokens: 2048

extract_graph:
  model_id: default_chat_model
  prompt: "prompts/extract_graph_autism.txt"
  entity_types: [biological_process, protein, disease, phenotype, anatomy, molecular_function, drug, cellular_component, pathway, exposure]
  max_gleanings: 1  # 增加提取轮数，确保实体关系完整性

extract_isolated_entity_relationships:
  model_id: default_chat_model
  prompt: "prompts/extract_isolated_entity_relationships.txt"
  max_gleanings: 1
  tuple_delimiter: "<|>"
  record_delimiter: "##"
  completion_delimiter: "<|COMPLETE|>"

summarize_descriptions:
  model_id: default_chat_model
  prompt: "prompts/summarize_descriptions.txt"
  max_length: 500

extract_graph_nlp:
  text_analyzer:
    extractor_type: regex_english # [regex_english, syntactic_parser, cfg]

cluster_graph:
  max_cluster_size: 10

extract_claims:
  enabled: false
  model_id: default_chat_model
  prompt: "prompts/extract_claims.txt"
  description: "Any claims or facts that could be relevant to information discovery."
  max_gleanings: 1

community_reports:
  model_id: default_chat_model
  graph_prompt: "prompts/community_report_graph.txt"
  text_prompt: "prompts/community_report_text.txt"
  max_length: 2000
  max_input_length: 8000

embed_graph:
  enabled: false # if true, will generate node2vec embeddings for nodes

umap:
  enabled: false # if true, will generate UMAP embeddings for nodes (embed_graph must also be enabled)

snapshots:
  graphml: false
  embeddings: false

### Query settings ###
## The prompt locations are required here, but each search method has a number of optional knobs that can be tuned.
## See the config docs: https://microsoft.github.io/graphrag/config/yaml/#query

local_search:
  chat_model_id: default_chat_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/local_search_system_prompt.txt"

global_search:
  chat_model_id: default_chat_model
  map_prompt: "prompts/global_search_map_system_prompt.txt"
  reduce_prompt: "prompts/global_search_reduce_system_prompt.txt"
  knowledge_prompt: "prompts/global_search_knowledge_system_prompt.txt"

drift_search:
  chat_model_id: default_chat_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/drift_search_system_prompt.txt"
  reduce_prompt: "prompts/drift_search_reduce_prompt.txt"

basic_search:
  chat_model_id: default_chat_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/basic_search_system_prompt.txt"
